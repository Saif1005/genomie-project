# ZAYNB backend — dépendances d'exécution (serveur local)

# API
fastapi>=0.135.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0,<3
python-multipart>=0.0.9

# Orchestration multi-agent (graphe planifier → exécuter)
langgraph>=0.2.0

# Utilitaires
loguru>=0.7.2
python-dotenv>=1.0.0
