# Commentaire BioGPT (optionnel, non décisionnel). torch est fourni par l'image Docker
# pytorch/pytorch ; hors Docker : pip install torch (CUDA ou CPU selon le serveur).
transformers>=4.36.2
peft>=0.7.1
sacremoses>=0.1.1
